# a scipt to see if cuda availablke for torch
import torch
# a script to see if cuda available for torch
import torch

cuda_available = torch.cuda.is_available()
print("CUDA is available:", cuda_available)
